# 构建状态

## Phase 0

- 状态：已在原生 Windows 项目环境中验收。
- 运行时：Python 3.12.14、PyTorch 2.11.0+cu128、CUDA 12.8。
- 设备：NVIDIA GeForce RTX 2060 6GB；CUDA 张量冒烟测试通过。
- 真实数据：本阶段按计划未下载。
- 已实现：包骨架、兼容 JSON 的 YAML 配置、确定性合成夹具、用户级划分、带掩码 BCE/F1、NumPy 线性冒烟模型、检查点、run 元数据、CLI、单元测试和集成测试。
- 阶段门：可编辑安装、测试、代码检查、依赖检查、CUDA 检查及冒烟命令全部通过。

合成指标只用于工程检查，绝不能作为研究结果。

## Phase 1

- 状态：已基于 ExtraSensory 官方归档在原生 Windows 上验收。
- 来源完整性：两个归档均有本地 SHA-256 记录；不声称为官方校验和。安全解压会拒绝路径穿越、绝对路径、链接、重复目标和无效 ZIP。
- 真实模式：60 个用户文件共享同一个 278 列表头，包括时间戳、225 个特征、51 个多标签目标和 `label_source` 元数据。
- 六模态模式：177 个选用列（`26/26/46/17/28/34`）和 48 个明确忽略列；无未映射或多重映射输入列。
- 审计：扫描 377,346 行；372,168 行可进入后续预处理；5,178 行的 15 个选用标签全部未知；没有记录缺少全部选用模态。
- 合同：全部 51 个标签列只含 `0/1/NaN`；`(user_id, timestamp)` 唯一；官方 fold 测试集及生成的 `36/12/12` 训练/验证/测试用户互斥。
- 验证：`14 passed`，Ruff 干净，完整审计成功退出并重建规约第 2.7 节全部产物。

## Phase 2

- 状态：真实数据 `dev` 协议已验收，使用外层 fold 0、验证 fold 1、种子 13 和一个 epoch；不是研究结果。
- 预处理：177 个特征；仅训练数据的中位数/IQR 和质量统计；保留原始特征/目标掩码；保存预处理器合同与哈希。
- 数据行：排除声明规则后，训练 232,827、验证 74,282、测试 65,059 条。
- 模型：全部六个 B0 单传感器变体、B1 线性早期融合、B2 MLP 早期融合和 B3 可用性掩码后期融合均生成完整 dev run 产物。
- 防泄漏：类别权重只拟合训练集，早停只读验证集，逐标签阈值只调节验证集，测试数据仅用于评估。
- 有效性：确定性 B1 重放完全一致；打乱标签后的验证 mAP 与平均标签流行率相差 0.004619；调参未读取测试 fold。
- 验证：单元/集成测试、Ruff、依赖检查、全部 run 产物检查、CLI 评估和 Phase 0 合成回归冒烟均通过。

## Phase 3

- 状态：真实数据 `dev` 协议已在 fold 0、种子 13、一个 epoch 下验收；不是研究结果。
- 模型：B4 内容门控融合、B5 污染训练门控融合、仅分类损失的质量感知 P，以及带可靠度/一致性辅助项的 P 均已完成。
- 污染：按样本和视图确定；缺失计数至少保留一个模态；Gaussian/bias/scale 噪声不修改标签，也不向模型暴露强度。
- 融合合同：不可用权重精确为零；可用权重和为一；推理无需人工故障元数据。
- 诊断：每个 run 保存逐样本/逐模态权重、可选可靠度、可用性，以及自然数据和固定污染探针的人工故障类型/强度。
- 验证：干净/缺失/噪声单元测试、确定性重放、全关拒绝、B2/B4/P 集成、官方 run 产物不变量、Ruff、依赖检查和早期阶段回归全部通过。

## Phase 4

- 状态：注册的 fold 0/seed 13 `dev` 评估矩阵已验收；不是可信研究结果。
- 评估：自然缺失使用全部合法行；受控评估使用 35,613 条六模态完整测试行中的确定性 4,096 ID 样本。
- 矩阵：验证 13 个模型；B2/B3/B4/B5/P 均完成冻结的 88 个受控场景，验证集阈值保持不变。
- 输出：E1–E8 场景与逐标签指标、宽格式受控预测、自然分层、模态权重/可靠度响应、相对退化、鲁棒性 AUC、效率、13 个聚合/来源表和 12 张图。
- 有效性：每个模型共享同一受控 ID 哈希；不可用指标保留为 `NaN`；fold/seed/run/配置/数据不匹配会使报告生成失败。
- 可复现性：两次生成的 28 个跟踪输出字节级一致；端到端夹具和完整回归测试通过。

## Phase 5

- 状态：冻结的五折、单种子可信协议已验收。
- 协议 SHA-256：`91a4e5fd7547ecf347d7fb3d7ee101d7389ac71f77386d4301e1bc1ec8929a7b`。
- 数据：五个官方外层 fold 均有独立的仅训练数据预处理器。
- 注册表：6/6 个开发消融单元和 60/60 个可信单元均在首次记录尝试中成功；失败/重试/跳过均为零。
- 治理：恢复时跳过成功单元；失败单元需要 `--retry-failed`；保留每次尝试和原因；`full` 以可信完成为前置条件。
- 结果：Robust Gated 在受控完整 Macro-F1 上以 0.6043 领先；Quality-Aware 在自然缺失 Macro-F1 上以 0.5781 领先，并在全部五个 fold 上超过 Gated。Sensor Dropout 降低多模态相对退化，但门控权重可解释性假设仅获部分支持。
- 报告：完整性验证接受全部 60 个可信 run；从 run 产物生成 13 个 E1–E8 表、11 张可信图、开发消融图、中文技术报告和来源映射。

## Phase 6

- 状态：原生 Windows 离线回放工作流已验收。
- 产物：Streamlit 页面加载相互匹配的可信检查点、预处理器、阈值、清单、模式和真实 fold 专属测试数据；合同违规时停止，绝不回退到伪造预测。
- 交互：支持随机/指定真实样本、自然可用性、用户控制模态移除、Gaussian/bias/scale 特征故障、预测差异、阈值、质量、可靠度、门控权重、B4/B5/P 对比和注册聚合背景。
- 隐私与范围：显示的用户身份再次哈希；页面醒目标明这是离线预计算特征回放，而非实时采集。
- 验证：真实 fold 0 样本在 audio 移除和 phone-state Gaussian 污染组合下通过 B4/B5/P 推理；不可用 audio 权重精确为零。Streamlit 健康端点返回 HTTP 200，两个演示测试和 Ruff 通过。

## Phase 7

- 状态：已按项目所有者确认后的范围验收。
- 范围调整：不制作 PPT；不创建或复现新的干净虚拟环境，改为在现有 `.venv-dev` 中执行完整终验。
- 最终材料：中文技术报告、README 声明级来源映射、数据卡、模型卡、限制文档、两分钟和八分钟口头稿、36 题答辩问题库、一页 Markdown/SVG 摘要、发布审查和最终审计报告均已完成。
- 当前环境终验：Python/PyTorch/CUDA/RTX 2060 冒烟、ExtraSensory 377,346 行全量审计、合成 prepare/train/evaluate、60/60 可信 run 报告重建、Streamlit HTTP 200 健康检查均通过。
- 报告可复现性：连续两次重建后比对 40 个报告文件，SHA-256 差异为零。
- 回归：全部 37 项测试通过；Ruff 与 `pip check` 通过。
- 剩余阶段工作：无。公开发布前仍需选择许可证并填写真实作者信息。
