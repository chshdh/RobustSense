# V2 配置命名空间

这里保存 RobustSense V2 的配置。V1 的 `configs/data/`、`configs/model/`、
`configs/corruption/`、`configs/experiment/` 和 `configs/evaluation/` 均保持只读。

Phase V2-0 只冻结 `phase_v2_0.yaml`。P2 模型、消融、混合故障和正式评估配置要在
对应阶段设计、测试、审查后再写入，不在本阶段预填未经实现验证的参数。

